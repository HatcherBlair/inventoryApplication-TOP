<div class="container-fluid text-center">
  <div class="row row-cols-auto">
    <% items.forEach(function(item) { %>
    <div class="col"><%-include("itemCard", {item: item});%></div>
    <% }); %>
  </div>
</div>
