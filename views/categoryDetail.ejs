<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/head') -%>
  </head>
  <body class="container bg-light">
    <header><%- include('./partials/header') -%></header>
    <div>
      <h5><%= category.name %></h5>
      <p><%= category.description %></p>
      <a
        href="/inventory/category/<%-category._id%>/update"
        class="btn btn-success"
        >Update</a
      >
      <a
        href="/inventory/category/<%-category._id%>/delete"
        class="btn btn-danger"
        >Delete</a
      >
    </div>
  </body>
</html>
